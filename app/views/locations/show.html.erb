
  <div class="page-header">
        <h1>設置場所</h1>
  </div>
  
 <h2>照会</h2>

<table class="table table-bordered table-hover ">
  <thead>
    <tr>
      <th>項目</th>
      <th>内容</th>
    </tr>
  </thead>

  <tbody>
  	<tr>
  		<td>会社名</td>
  		<td><%= @location.company.name %></td>
  	</tr>
  	<tr>
  		<td>製造コード</td>
  		<td><%= @location.device.uuid %></td>
  	</tr>
  	<tr>
  		<td>部署</td>
  		<td><%= @location.department %></td>
  	</tr>
  	<tr>
  		<td>場所</td>
  		<td><%= @location.place %></td>
  	</tr>

    <tr>
      <td>登録日</td>
      <td><%= @location.created_at %></td>
    </tr>
  </tbody>
</table>

<%= link_to '編集', edit_location_path(@location)  ,{:class => "btn btn-primary btn"} %>
<%= link_to t('.cancel', :default => t("戻る")), locations_path, :class => 'btn btn-default btn' %>
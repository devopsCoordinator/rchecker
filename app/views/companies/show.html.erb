  <div class="page-header">
        <h1>会社</h1>
  </div>
  
 <h2>照会</h2>

<table class="table table-bordered table-hover ">
  <thead>
    <tr>
      <th>項目</th>
      <th>内容</th>
    </tr>
  </thead>

  <tbody>
  	<tr>
  		<td>会社名</td>
  		<td><%= @company.name %></td>
  	</tr>
  	  	<tr>
  		<td>地域コード</td>
  		<td><%= @company.area.name %></td>
  	</tr>
  	  	<tr>
  		<td>担当者</td>
  		<td><%= @company.representative.name %></td>
  	</tr>
  </tbody>
</table>

<%= link_to '編集', edit_company_path(@company)  ,{:class => "btn btn-primary btn"} %>
<%= link_to t('.cancel', :default => t("戻る")), companies_path, :class => 'btn btn-default btn' %>
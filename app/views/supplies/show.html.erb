  <div class="page-header">
    <h1>配給履歴</h1>
  </div>

<h2><%= @company.name %>の「<%= @device.uuid %>」の配給履歴</h2>
<p>ボトルが空になるまでの平均時間 「<%= @elapsed_average_day %> 日 <%= @elapsed_average_hour %> 時 <%= @elapsed_average_min %> 分 <%= @elapsed_average_sec %>秒 」</p>
<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>#</th>
      <th>開始日時</th>
      <th>終了日時</th>
      <th>経過時間</th>
    </tr>
  </thead>

  <tbody>
    <% @supplies.each do |supply| %>
      <tr>
        <td><%= supply.id %></td>
        <td><%= supply.start_date %></td>
        <td><%= supply.end_date %></td>
        <% if supply.elapsed_time.present? %>
        <td><%= supply.elapsed_time_day %> 日 <%= supply.elapsed_time_hour %> 時間 <%= supply.elapsed_time_min %> 分 <%= supply.elapsed_time_sec %> 秒</td>
        <% else %>
                <td>-</td>
        <% end%>
      </tr>
    <% end %>
  </tbody>
</table>
